/**
 * Domain Types - Universal Data Contracts
 * Following Domain-Driven Design principles with tool-agnostic naming.
 * 
 * This is the canonical domain model for the HVAC AI Platform.
 * Uses universal terminology rather than implementation-specific names.
 */

// Re-export from analysis.ts for backward compatibility
export type { Segment, CountResult, AnalysisResult } from './analysis';
