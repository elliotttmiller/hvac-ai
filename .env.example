# ==============================================================================
# HVAC AI - LOCAL DEVELOPMENT ENVIRONMENT (.env.example)
# ==============================================================================
# Copy this file to .env.local and fill in your actual values.
# Secrets and keys have been redacted below for security.

# ------------------------------------------------------------------------------
# [1] FRONTEND CONFIGURATION (PUBLIC - Prefixed with NEXT_PUBLIC_)
# ------------------------------------------------------------------------------

# The absolute URL where the Next.js app will be running. Required by NextAuth.
NEXTAUTH_URL="http://localhost:3000"

# The base URL for the AI backend. This is what the frontend will call.
# During local development, this points to your local Ray Serve instance.
NEXT_PUBLIC_API_BASE_URL="http://127.0.0.1:8000"


# ------------------------------------------------------------------------------
# [2] BACKEND CONFIGURATION (PRIVATE - Read by Python scripts)
# ------------------------------------------------------------------------------

# --- Core Service Ports ---
# Port for the Ray Serve HTTP server (Backend API)
BACKEND_PORT="8000"
# Port for the Next.js development server (Frontend UI)
FRONTEND_PORT="3000"

# --- AI Model Configuration ---
# Absolute or relative path to your trained YOLOv11-OBB model weights.
# Example (absolute): D:\AMD\hvac-ai\ai_model\models\hvac_obb_l_20251224_214011\weights\best.pt
# Example (relative): ./ai_model/models/hvac_obb_l_20251224_214011/weights/best.pt
MODEL_PATH="./ai_model/models/hvac_obb_l_20251224_214011/weights/best.pt"

# --- Ray Serve Cluster Configuration ---
# Address of the Ray head node. Leave empty to start a new local cluster.
# Example for connecting to an existing cluster: "ray://192.168.1.100:10001"
RAY_ADDRESS=""

# --- Development & Debug Flags ---
# Set to "1" to start the backend without loading heavy AI models (for UI-only dev).
SKIP_MODEL="0"
# Set to "1" to force all AI models to run on the CPU, even if a GPU is available.
FORCE_CPU="0"
# Set to "1" to enable GPU resource allocation in Ray.
RAY_USE_GPU="1"


# ------------------------------------------------------------------------------
# [3] SECURITY & AUTHENTICATION (PRIVATE)
# ------------------------------------------------------------------------------

# A secret key used by NextAuth for signing sessions and tokens.
# Generate a strong secret: `openssl rand -hex 32` (on Linux/Mac) or
# `[Convert]::ToBase64String((1..32|ForEach-Object{Get-Random -Max 256})) | Out-String` (on PowerShell)
NEXTAUTH_SECRET="your-nextauth-secret-key-here"

# Secure tunneling token for ngrok (optional, for external access in dev).
# Get your token from https://dashboard.ngrok.com/get-started/your-authtoken
NGROK_AUTHTOKEN="your-ngrok-authtoken-here"

